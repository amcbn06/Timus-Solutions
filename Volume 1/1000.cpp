/* 1000. A + B

Strategy:
Just summate the numbers

* Time complexity: O(1)
* Space complexity: O(1)

*/
#include <bits/stdc++.h>
using namespace std;

long long a, b;

int main() {
	ios::sync_with_stdio(0);
	cin >> a >> b;
	cout << a + b;
}
